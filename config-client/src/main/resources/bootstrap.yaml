spring:
  application:
    name: my-config
  cloud:
    config:
      profile: test
      label: master
      discovery:
        enabled: true
        service-id: config-server
#      uri: http://localhost:9884
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9881/eureka,http://localhost:9880/eureka


